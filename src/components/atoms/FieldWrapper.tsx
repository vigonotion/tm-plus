import { styled } from "@styled-system/jsx";

export const FieldWrapper = styled("div", {
  base: {
    gap: 0,
    display: "flex",
    flexDirection: "column",
    alignItems: "stretch",
  },
  variants: {},
});
